@model SupplierViewModel
@{
    ViewData["Title"] = "Delete Supplier";
}

<h4 class="page-heading"><i class="bi bi-people"></i> @ViewData["Title"]</h4>

<div class="delete-confirm d-flex align-items-center gap-3">
    <i class="bi bi-exclamation-triangle-fill fs-3"></i>
    <div>
        <strong>Are you sure?</strong><br/>
        <span class="text-muted">This action cannot be undone. All expenses linked to this supplier will remain.</span>
    </div>
</div>

<div class="row">
    <div class="col-md-6">
        <dl class="row">
            <dt class="col-sm-4">Supplier Name</dt>
            <dd class="col-sm-8">@Model.SupplierName</dd>

            <dt class="col-sm-4">Location</dt>
            <dd class="col-sm-8">@Model.Location</dd>

            <dt class="col-sm-4">VAT No</dt>
            <dd class="col-sm-8">@Model.VatNo</dd>
        </dl>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="SupplierId" />
            <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Delete</button>
            <a asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-x-lg"></i> Cancel</a>
        </form>
    </div>
</div>
