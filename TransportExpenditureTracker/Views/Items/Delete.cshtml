@model ItemViewModel
@{
    ViewData["Title"] = "Delete Item";
}

<h4 class="page-heading"><i class="bi bi-box"></i> @ViewData["Title"]</h4>

<div class="delete-confirm d-flex align-items-center gap-3">
    <i class="bi bi-exclamation-triangle-fill fs-3"></i>
    <div>
        <strong>Are you sure?</strong><br/>
        <span class="text-muted">This action cannot be undone. Existing expense entries referencing this item will remain.</span>
    </div>
</div>

<div class="row">
    <div class="col-md-6">
        <dl class="row">
            <dt class="col-sm-4">Item Name</dt>
            <dd class="col-sm-8">@Model.ItemName</dd>

            <dt class="col-sm-4">Unit</dt>
            <dd class="col-sm-8">@Model.Unit</dd>
        </dl>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="ItemId" />
            <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Delete</button>
            <a asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-x-lg"></i> Cancel</a>
        </form>
    </div>
</div>
